# 架空デモ：伊勢様タイプの月次SOAP（2026年4月）

**実在の利用者ではありません。** 報告書要約・計画照合・褥瘡抽出の**動作確認用**にのみ使います。

本番の伊勢様データは `data/patients/{id}/`（Git 外）に置き、同じファイル命名で

`python operations/monthly_clinical_automation.py ... --soap-glob "path/*.md"`

のように指定してください。
